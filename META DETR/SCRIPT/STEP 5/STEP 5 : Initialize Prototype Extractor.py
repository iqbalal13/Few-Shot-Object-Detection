# ==========================================================
# STEP 5 : Initialize Prototype Extractor
# ==========================================================

prototype_extractor = PrototypeFeatureExtractor().to(CONFIG["device"])

print(prototype_extractor)
